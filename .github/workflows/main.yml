name: Call External Skill

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Call skill from Repo B
        uses: my-org/skill-repo@main
        with:
          name: Himanshi